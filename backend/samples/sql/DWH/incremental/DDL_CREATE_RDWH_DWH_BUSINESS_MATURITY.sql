DROP TABLE DWH_BUSINESS_MATURITY IF EXISTS;

CREATE TABLE DWH_BUSINESS_MATURITY
(
    ID           SMALLINT     NOT NULL,
    CODE         VARCHAR(32)  NOT NULL,
    NAME         VARCHAR(64)  NOT NULL,
    CREATED_DATE TIMESTAMP(6) DEFAULT CURRENT TIMESTAMP,
    CONSTRAINT PK_DWH_BUSINESS_MATURITY
    PRIMARY KEY (ID)
)
     ORGANIZE BY COLUMN IN TBS_<env> DISTRIBUTE ON RANDOM;