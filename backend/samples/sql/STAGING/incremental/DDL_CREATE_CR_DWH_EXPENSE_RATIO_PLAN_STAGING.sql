SET SCHEMA STAGING_<env>;

DROP TABLE DWH_EXPENSE_RATIO_PLAN_STAGING IF EXISTS;

CREATE TABLE DWH_EXPENSE_RATIO_PLAN_STAGING
(
    
    LINE_NUMBER             VARCHAR(255),
    VERSION_YEAR            VARCHAR(255),
    VERSION_QUARTER         VARCHAR(255),
    VERSION_TYPE            VARCHAR(255),
    PRODUCT_LINE            VARCHAR(255),
    SEGMENT_CODE            VARCHAR(255),
    SEGMENT_LABEL           VARCHAR(255),
    EXPENSE_RATIO_TYPE      VARCHAR(255),
    CURRENCY_CODE           VARCHAR(255),
    ALLOCATED_AMOUNT        VARCHAR(255),
    EXPENSE_RATIO           VARCHAR(255)
)
    ORGANIZE BY COLUMN IN TBS_<env> DISTRIBUTE ON RANDOM;