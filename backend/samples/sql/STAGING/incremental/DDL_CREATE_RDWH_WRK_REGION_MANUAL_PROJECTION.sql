SET SCHEMA STAGING_<env>;

DROP TABLE WRK_REGION_MANUAL_PROJECTION IF EXISTS;

CREATE TABLE WRK_REGION_MANUAL_PROJECTION
(
    OMEGA_TREATY_NUMBER CHARACTER(9) NOT NULL,
    OMEGA_SECTION       INTEGER      NOT NULL,
    P_SPLIT             VARCHAR(100),
    AOC_STEP            VARCHAR(100) NOT NULL,
    SENSITIVITY_TYPE    VARCHAR(100) NOT NULL,
    SENSIVITY_VALUE     DECFLOAT     NOT NULL,
    POLICY_UWY          VARCHAR(4),
    P_POSITION          VARCHAR(100) NOT NULL,
    CURRENCY            VARCHAR(3)   NOT NULL,
    BASIS               VARCHAR(100) NOT NULL,
    PERIOD_DESCRIPTION  VARCHAR(10),
    PROJECTION_MONTH    INTEGER      NOT NULL,
    PROJECTION_YEAR     INTEGER      NOT NULL,
    AMOUNT              DECFLOAT     NOT NULL,
    CREATED_BY          VARCHAR(20)  NOT NULL,
    CREATED_DATE        TIMESTAMP(6) NOT NULL,
    LINE_NUMBER         BIGINT,
    REQUEST_ID          BIGINT,
    HISTORIZATION_MODE  VARCHAR(20)  NOT NULL,
    CLOSING_DATE        DATE         NOT NULL,
    BUSINESS_MATURITY   VARCHAR(50)
)
    ORGANIZE BY COLUMN IN TBS_<env> DISTRIBUTE ON RANDOM;