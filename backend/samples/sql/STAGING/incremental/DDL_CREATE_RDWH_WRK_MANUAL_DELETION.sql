SET SCHEMA STAGING_<env>;

DROP TABLE WRK_MANUAL_DELETION IF EXISTS;

CREATE TABLE WRK_MANUAL_DELETION (
	REQUEST_ID      INTEGER NOT NULL,
	HASH_KEY_DELTA  VARBINARY(64) NOT NULL
)
ORGANIZE BY COLUMN IN TBS_<env> DISTRIBUTE ON RANDOM;