SET SCHEMA STAGING_<env>;

DROP TABLE WRK_F_G_MMIND_PROJECTIONS IF EXISTS;

CREATE TABLE WRK_F_G_MMIND_PROJECTIONS
(
    CLOSING_DATE             DATE          NOT NULL,
    MODELLING_REGION         VARCHAR(50)   NOT NULL,
    PRODUCTIVE_NONPRODUCTIVE VARCHAR(100)  NOT NULL,
    CONTRACT_NUMBER          CHARACTER(9)  NOT NULL,
    SECTION_NUMBER           DECIMAL(2)    NOT NULL,
    ASSUMED_CONTRACT_NUMBER  CHARACTER(9),
    ASSUMED_SECTION_NUMBER   DECIMAL(2),
    AOC_STEP                 VARCHAR(100)  NOT NULL,
    SENSITIVITY_TYPE         VARCHAR(100)  NOT NULL,
    SENSITIVITY_VALUE        DECIMAL(10,6) NOT NULL,
    POLICY_UWY               DECIMAL(4),
    BUSINESS_MATURITY        VARCHAR(50)   NOT NULL,
    POSITION                 VARCHAR(100)  NOT NULL,
    CURRENCY                 VARCHAR(3)    NOT NULL,
    REPORTING_BASIS_TYPE     VARCHAR(100)  NOT NULL,
    PERIOD_DESCRIPTION       VARCHAR(10)   NOT NULL,
    PROJECTION_MONTH         INTEGER       NOT NULL,
    PROJECTION_YEAR          INTEGER       NOT NULL,
    PROJECTION_AMOUNT        DECFLOAT(16),
    CREATED_BY               VARCHAR(20)   NOT NULL,
    CREATED_DATE             TIMESTAMP(6),
    LINE_NUMBER              BIGINT,
    REQUEST_ID				 BIGINT
)
    ORGANIZE BY COLUMN IN TBS_<env> DISTRIBUTE ON RANDOM;