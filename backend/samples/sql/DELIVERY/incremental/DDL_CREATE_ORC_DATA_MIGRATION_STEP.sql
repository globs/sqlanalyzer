DROP TABLE DATA_MIGRATION_STEP IF EXISTS;
CREATE TABLE DATA_MIGRATION_STEP
(
	STEP_ID	                INT NOT NULL,
	DWH_SCHEMA_NAME         VARCHAR(128)	NOT NULL,
	DWH_TABLE_NAME          VARCHAR(128)	NOT NULL,
	MIGRATION_QUERY         VARCHAR(10000)	NOT NULL,
	MIGRATION_MODE	        VARCHAR(16)
) ORGANIZE BY ROW DISTRIBUTE ON RANDOM IN TBS_<env>;
ALTER TABLE DATA_MIGRATION_STEP ADD CONSTRAINT DATA_MIGRATION_STEP_PK PRIMARY KEY (STEP_ID);