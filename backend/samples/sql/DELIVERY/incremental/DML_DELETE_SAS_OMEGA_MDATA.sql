DELETE FROM DATA_FETCH_STEP WHERE STEP_ID IN (
SELECT STEP_ID FROM STEP WHERE JOB_ID IN (
SELECT JOB_ID FROM JOB WHERE JOB_GROUP_ID IN (
SELECT JOB_GROUP_ID FROM JOB_GROUP WHERE BATCH_ID IN (
SELECT BATCH_ID FROM BATCH WHERE BATCH_NAME IN (
'DIP to SAS Run', 'SAS to DIP Run', 'DIP to Omega Run (SAS Results)')))));

DELETE FROM STAGING_LOAD_STEP WHERE STEP_ID IN (
SELECT STEP_ID FROM STEP WHERE JOB_ID IN (
SELECT JOB_ID FROM JOB WHERE JOB_GROUP_ID IN (
SELECT JOB_GROUP_ID FROM JOB_GROUP WHERE BATCH_ID IN (
SELECT BATCH_ID FROM BATCH WHERE BATCH_NAME IN (
'DIP to SAS Run', 'SAS to DIP Run', 'DIP to Omega Run (SAS Results)')))));

DELETE FROM DWH_LOAD_STEP WHERE STEP_ID IN (
SELECT STEP_ID FROM STEP WHERE JOB_ID IN (
SELECT JOB_ID FROM JOB WHERE JOB_GROUP_ID IN (
SELECT JOB_GROUP_ID FROM JOB_GROUP WHERE BATCH_ID IN (
SELECT BATCH_ID FROM BATCH WHERE BATCH_NAME IN (
'DIP to SAS Run', 'SAS to DIP Run', 'DIP to Omega Run (SAS Results)')))));

DELETE FROM DATA_EXPOSITION_STEP WHERE STEP_ID IN (
SELECT STEP_ID FROM STEP WHERE JOB_ID IN (
SELECT JOB_ID FROM JOB WHERE JOB_GROUP_ID IN (
SELECT JOB_GROUP_ID FROM JOB_GROUP WHERE BATCH_ID IN (
SELECT BATCH_ID FROM BATCH WHERE BATCH_NAME IN (
'DIP to SAS Run', 'SAS to DIP Run', 'DIP to Omega Run (SAS Results)')))));


DELETE FROM STEP WHERE JOB_ID IN (
SELECT JOB_ID FROM JOB WHERE JOB_GROUP_ID IN (
SELECT JOB_GROUP_ID FROM JOB_GROUP WHERE BATCH_ID IN (
SELECT BATCH_ID FROM BATCH WHERE BATCH_NAME IN (
'DIP to SAS Run', 'SAS to DIP Run', 'DIP to Omega Run (SAS Results)'))));

DELETE FROM JOB_DEPENDENCY WHERE JOB_ID IN (
SELECT JOB_ID FROM JOB WHERE JOB_GROUP_ID IN (
SELECT JOB_GROUP_ID FROM JOB_GROUP WHERE BATCH_ID IN (
SELECT BATCH_ID FROM BATCH WHERE BATCH_NAME IN (
'DIP to SAS Run', 'SAS to DIP Run', 'DIP to Omega Run (SAS Results)'))));


DELETE FROM JOB WHERE JOB_GROUP_ID IN (
SELECT JOB_GROUP_ID FROM JOB_GROUP WHERE BATCH_ID IN (
SELECT BATCH_ID FROM BATCH WHERE BATCH_NAME IN (
'DIP to SAS Run', 'SAS to DIP Run', 'DIP to Omega Run (SAS Results)')));


DELETE FROM JOB_GROUP WHERE BATCH_ID IN (
SELECT BATCH_ID FROM BATCH WHERE BATCH_NAME IN (
'DIP to SAS Run', 'SAS to DIP Run', 'DIP to Omega Run (SAS Results)'));

DELETE FROM DATA_VALIDATION_CONTROL WHERE CONTROL_NAME IN (
'SAS AE Results Amounts', 'Omega AE Results Amounts');

DELETE FROM DATA_VALIDATION_CONTROL_MAPPING WHERE BATCH_ID IN (
SELECT BATCH_ID FROM BATCH WHERE BATCH_NAME IN (
'DIP to SAS Run', 'SAS to DIP Run', 'DIP to Omega Run (SAS Results)'));

DELETE FROM BATCH WHERE BATCH_NAME IN (
'DIP to SAS Run', 'SAS to DIP Run', 'DIP to Omega Run (SAS Results)');

DELETE FROM BATCH_GROUPING WHERE BATCH_GROUPING_NAME = 'DIP to SAS PROCESSING';

DELETE FROM BUSINESS_EVENT_PARAM_VALUATION;

DELETE FROM ORCHESTRATION_PROCESS_STEP_DEPENDENCY WHERE ORCHESTRATION_PROCESS_STEP_ID IN (
SELECT ORCHESTRATION_PROCESS_STEP_ID FROM ORCHESTRATION_PROCESS_STEP WHERE ORCHESTRATION_PROCESS_ID IN (
SELECT ORCHESTRATION_PROCESS_ID FROM ORCHESTRATION_PROCESS WHERE ORCHESTRATION_PROCESS_NAME = 'Orchestration Process for DIP to SAS to DIP data flow'));


DELETE FROM ORCHESTRATION_PROCESS_STEP WHERE ORCHESTRATION_PROCESS_ID IN (
SELECT ORCHESTRATION_PROCESS_ID FROM ORCHESTRATION_PROCESS WHERE ORCHESTRATION_PROCESS_NAME = 'Orchestration Process for DIP to SAS to DIP data flow');

DELETE FROM ORCHESTRATION_PROCESS WHERE ORCHESTRATION_PROCESS_NAME = 'Orchestration Process for DIP to SAS to DIP data flow';

DELETE FROM EVENT_INSTANCE WHERE EVENT_CODE IN ('EVT_DIP_TO_SAS_DATA', 'EVT_SAS_TO_DIP_RESULTS', 'EVT_DIP_TO_OMEGA_RUN_FOR_SAS_RESULTS');

DELETE FROM EXECUTION_KEY_VALUE_METADATA WHERE "KEY" IN ('EVT_DIP_TO_SAS_DATA', 'EVT_SAS_TO_DIP_RESULTS', 'EVT_DIP_TO_OMEGA_RUN_FOR_SAS_RESULTS');

DELETE FROM EVENT_MAPPING WHERE EVENT_CODE IN ('EVT_DIP_TO_SAS_DATA', 'EVT_SAS_TO_DIP_RESULTS', 'EVT_DIP_TO_OMEGA_RUN_FOR_SAS_RESULTS');

DELETE FROM EVENT WHERE EVENT_CODE IN ('EVT_DIP_TO_SAS_DATA', 'EVT_SAS_TO_DIP_RESULTS', 'EVT_DIP_TO_OMEGA_RUN_FOR_SAS_RESULTS');

COMMIT;