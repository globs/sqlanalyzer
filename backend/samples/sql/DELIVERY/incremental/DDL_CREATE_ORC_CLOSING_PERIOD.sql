DROP TABLE CLOSING_PERIOD IF EXISTS;
CREATE TABLE CLOSING_PERIOD
(
	CLOSING_PERIOD_ID	                    INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	CLOSING_PERIOD		                    VARCHAR(32) NOT NULL,
	CLOSING_PERIOD_NAME				        VARCHAR(128),
	CLOSING_TYPE_CODE						VARCHAR(32),
	SCENARIO_TYPE			 	    		VARCHAR(32),
	CLOSING_DATE						    TIMESTAMP,
	CLOSING_YEAR						    SMALLINT,
	CLOSING_QUARTER						    VARCHAR(32),
	STATUS									VARCHAR(16),
	START_DATE						        TIMESTAMP,
    END_DATE						        TIMESTAMP	
) ORGANIZE BY ROW DISTRIBUTE ON RANDOM IN TBS_<env>;
ALTER TABLE CLOSING_PERIOD ADD CONSTRAINT CLOSING_PERIOD_PK PRIMARY KEY (CLOSING_PERIOD_ID);
