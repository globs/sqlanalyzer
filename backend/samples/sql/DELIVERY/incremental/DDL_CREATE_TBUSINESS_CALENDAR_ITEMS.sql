DROP TABLE TBUSINESS_CALENDAR_ITEMS IF EXISTS;

CREATE TABLE TBUSINESS_CALENDAR_ITEMS
(
    CALEND_ITEM_ID    INTEGER       NOT NULL,
    CALEND_ITEM_LB    VARCHAR(255)  NOT NULL,
    APP_ID            SMALLINT      NOT NULL,
    DATA_WAY          VARCHAR(3)    NOT NULL,
    ITEM_STATUS       VARCHAR(55)   NOT NULL,
    IS_ACTIVE         CHARACTER(1)  NOT NULL,
    TRIGGER_MODE      VARCHAR(55)   NOT NULL,
    EVENT_ID          INTEGER,
    START_DATE        TIMESTAMP(6)  NOT NULL,
    DELAY_DAYS        INTEGER       NOT NULL,
    BUSINESS_UNIT     VARCHAR(10)   NOT NULL,
    LINKED_CMD        VARCHAR(1024),
    END_DATE          TIMESTAMP(6),
    UPLOAD_REQUEST_ID BIGINT,
    BATCH_ID          INTEGER,
    CONSTRAINT PK_TBUSINESS_CALENDAR_ITEMS PRIMARY KEY (CALEND_ITEM_ID)
)
    ORGANIZE BY COLUMN IN TBS_<env> DISTRIBUTE ON RANDOM;