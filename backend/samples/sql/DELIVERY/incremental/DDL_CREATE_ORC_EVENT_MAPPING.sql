DROP TABLE EVENT_MAPPING IF EXISTS;
CREATE TABLE EVENT_MAPPING
(
	EVENT_CODE	                   VARCHAR(128) NOT NULL,
	BATCH_ID	                   INT,
	ORCHESTRATION_PROCESS_STEP_ID  INT,
	ORCHESTRATION_PROCESS_ID       INT,
	BATCH_GROUPING_ID			   INT,
	IS_DWH_SWITCH				   BOOLEAN NOT NULL,
	DELIMITER 					   CHAR(1)
) ORGANIZE BY ROW DISTRIBUTE ON RANDOM IN TBS_<env>;
ALTER TABLE EVENT_MAPPING ADD CONSTRAINT EVENT_MAPPING_PK PRIMARY KEY (EVENT_CODE);
