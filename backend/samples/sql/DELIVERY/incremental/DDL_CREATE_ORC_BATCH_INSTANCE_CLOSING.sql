DROP TABLE BATCH_INSTANCE_CLOSING IF EXISTS;
CREATE TABLE BATCH_INSTANCE_CLOSING
(
	BATCH_INSTANCE_ID	                INT NOT NULL,
	CLOSING_TYPE_CODE                   VARCHAR(32),
	CLOSING_DATE                        TIMESTAMP,
	CLOSING_PERIOD						VARCHAR(32)
) ORGANIZE BY ROW DISTRIBUTE ON RANDOM IN TBS_<env>;
