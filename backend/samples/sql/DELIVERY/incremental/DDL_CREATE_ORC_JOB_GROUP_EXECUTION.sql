DROP TABLE JOB_GROUP_EXECUTION IF EXISTS;
CREATE TABLE JOB_GROUP_EXECUTION
(
	JOB_GROUP_EXECUTION_ID	             INT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1) NOT NULL,
	BATCH_EXECUTION_ID	                 INT NOT NULL,
	JOB_GROUP_ID	                     INT NOT NULL,
	CREATE_TIME	                         TIMESTAMP NOT NULL,
	START_TIME	                         TIMESTAMP,
	END_TIME	                         TIMESTAMP,
	STATUS	                             VARCHAR(16),
	EXIT_CODE	                         VARCHAR(16),
	EXIT_MESSAGE	                     VARCHAR(10000)
) ORGANIZE BY ROW DISTRIBUTE ON RANDOM IN TBS_<env>;
ALTER TABLE JOB_GROUP_EXECUTION ADD CONSTRAINT JOB_GROUP_EXECUTION_PK PRIMARY KEY (JOB_GROUP_EXECUTION_ID);