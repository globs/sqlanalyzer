DROP TABLE JOB_GROUP IF EXISTS;
CREATE TABLE JOB_GROUP
(
	JOB_GROUP_ID	        INT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	JOB_GROUP_NAME	        VARCHAR(128) NOT NULL,
	BATCH_ID	            INT NOT NULL,
	PARENT_JOB_GROUP_ID	    INT
) ORGANIZE BY ROW DISTRIBUTE ON RANDOM IN TBS_<env>;
ALTER TABLE JOB_GROUP ADD CONSTRAINT JOB_GROUP_PK PRIMARY KEY (JOB_GROUP_ID);