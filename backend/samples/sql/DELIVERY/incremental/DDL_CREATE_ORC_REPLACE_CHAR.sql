DROP TABLE REPLACE_CHAR IF EXISTS;
CREATE TABLE REPLACE_CHAR
(
	CHAR_TO_BE_REPLACED	          CHAR(1) NOT NULL,
	REPLACED_BY	                  CHAR(1)
) ORGANIZE BY ROW DISTRIBUTE ON RANDOM IN TBS_<env>;
ALTER TABLE REPLACE_CHAR ADD CONSTRAINT REPLACE_CHAR_PK PRIMARY KEY (CHAR_TO_BE_REPLACED);