DROP TABLE JOB_DEPENDENCY IF EXISTS;
CREATE TABLE JOB_DEPENDENCY
(
	JOB_ID	           INT NOT NULL,
	PARENT_JOB_ID	   INT NOT NULL
) ORGANIZE BY ROW DISTRIBUTE ON RANDOM IN TBS_<env>;
ALTER TABLE JOB_DEPENDENCY ADD CONSTRAINT JOB_DEPENDENCY_PK PRIMARY KEY (JOB_ID, PARENT_JOB_ID);